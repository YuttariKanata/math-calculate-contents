# Decimal Expansion of $9^{9^{9}}$

このファイルは、$9^{9^{9}}$（超指数 / Tetration $^{3}9$）の十進展開に関する計算結果の参照用ドキュメントです。

## 1. データの概要
- **総桁数:** 369,693,100 桁
- **ファイルサイズ:** 約 353 MB
- **形式:** UTF-8 プレーンテキスト (.txt)
- **計算完了日:** 2026年3月 (推定)

## 2. データへのアクセス
ファイルサイズが GitHub の上限（100MB）を超えているため、外部ストレージ（Google Drive）にホストしています。

- **[Download Result (Google Drive)](https://drive.google.com/file/d/1UZFBr4ndeM5aO4dayIDVlOjZ-gLBShcx/view?usp=drive_link)**

> [!IMPORTANT]
> Google Driveのビューアではファイルが巨大すぎて正常に表示されないことがあります。中身を確認する際は、ローカルにダウンロードし、巨大ファイルに対応したエディタ（VS Code, Emacs, または `less` コマンド等）を使用してください。

## 3. 計算アルゴリズムと環境
後学のため、および再現性確保のために計算時の詳細を記録しています。

### 計算環境
- **言語:** Julia 1.10.x
- **ライブラリ:** `BigInt` (GNU MPFR)
- **ハードウェア:** [あなたのPC環境、あるいは大学の計算機環境など]

### 実装概要
`BigInt` を用いた直接計算を行い、`IOStream` を介してバッファリング出力を実施。

```julia
# 計算・出力コードのコアロジック
open("9_9_9.txt", "w") do io
    print(io, BigInt(9)^(BigInt(9)^9))
end
```

## 4\. 検証状況

  - [x] 桁数一致確認 ($369,693,100$ 桁)
  - [ ] 末尾の数桁の検証 ($9^{9^9} \pmod{10^k}$)
  - [ ] 他のアルゴリズム（例：バイナリ累乗法）との一致確認

-----

[Return to Repository Top](https://github.com/YuttariKanata/math-calculate-contents)